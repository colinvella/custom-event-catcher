{
  "name": "custom-event-catcher",
  "version": "0.1.0",
  "description": "Chrome extension to capture CustomEvent dispatches and log them",
  "author": "Colin Vella",
  "scripts": {
    "prebuild": "node ./scripts/clean.js",
    "build": "tsc -p tsconfig.json && npm run bundle:all && node ./scripts/copy-assets.js",
    "watch": "tsc -p tsconfig.json -w",
    "bundle:esm": "esbuild src/background.ts src/panel.ts src/popup.ts src/devtools.ts --bundle --format=esm --platform=browser --target=es2020 --outdir=dist --sourcemap",
    "bundle:iife": "esbuild src/content-script.ts src/inject.ts --bundle --format=iife --platform=browser --target=es2020 --outdir=dist --sourcemap",
    "bundle:all": "npm run bundle:esm && npm run bundle:iife",
    "zip": "node ./scripts/zip.js",
    "package": "npm run build && npm run zip"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.27",
    "esbuild": "^0.21.5",
    "sharp": "^0.34.5",
    "typescript": "^5.2.2"
  }
}