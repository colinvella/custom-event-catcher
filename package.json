{
  "name": "custom-event-catcher",
  "version": "0.2.0",
  "description": "Chrome extension to capture CustomEvent dispatches and log them",
  "author": "Colin Vella",
  "scripts": {
    "prebuild": "node ./scripts/clean.js",
    "build": "tsc -p tsconfig.json && npm run bundle:all && node ./scripts/copy-assets.js",
    "watch": "tsc -p tsconfig.json -w",
  "bundle:esm": "esbuild src/background/background.ts src/panel/panel.ts src/popup/popup.ts src/devtools/devtools.ts --bundle --format=esm --platform=browser --target=es2020 --outdir=dist --sourcemap",
    "bundle:iife": "esbuild src/content-script/content-script.ts src/inject/inject.ts --bundle --format=iife --platform=browser --target=es2020 --outdir=dist --sourcemap",
    "bundle:all": "npm run bundle:esm && npm run bundle:iife",
    "zip": "node ./scripts/zip.js",
    "package": "npm run build && npm run zip"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.27",
    "esbuild": "^0.21.5",
    "sharp": "^0.34.5",
    "typescript": "^5.2.2"
  }
}